export type HomeInfoItem = {
  id: string;
  title: string;
  description: string;
  icon?: React.ReactNode;
};

export type CardInfoItem = {
  id: string;
  title: string;
  description: string;
  icon?: React.ReactNode;
  link?: string;
};